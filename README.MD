This repo contains my personal dotfiles configuration.

To setup this config on a new machine, clone this repo to `~/dotfiles` and run `stow .`. 
(GNU Stow can be installed via Homebrew with `brew install stow`.)


Work-specific config lives outside of this repo entirely and is not sym-linked using GNU Stow.
